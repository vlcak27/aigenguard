# Precision Fixture: Placeholder Credentials Text

This fixture keeps fake placeholder credential-shaped text in test data. It must
not produce secret leak findings or emit those placeholder values as raw leaks.
