# Precision Fixture: README Example Only

This fixture proves copy-paste examples and placeholders are not leaked secrets.

Example:

```sh
export OPENAI_API_KEY=<OPENAI_API_KEY>
curl https://api.example.invalid/v1/chat
```

The token above is a placeholder, not a real credential value.
