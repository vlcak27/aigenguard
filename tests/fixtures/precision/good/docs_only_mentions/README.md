# Precision Fixture: Docs Only Mentions

This safe fixture exists to prove documentation words are not executable evidence.

The words shell, bash, curl, OPENAI_API_KEY, MCP, and subprocess appear here as
documentation text only. There is no agent code, no MCP config, and no stored
credential value.
